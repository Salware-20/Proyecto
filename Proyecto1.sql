create database Registro;
use Registro;

create table Person (
 id varchar(50),
 Nombre varchar(50),
 Apellido Varchar(50),
 Correo varchar(50),
 Clave Varchar(50),
 Confirmar Varchar(50)

)

select * from Person;
